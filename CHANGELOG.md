# 更新记录

记录面向使用者的仓库变化。项目内部的 `production_contract_version`、`house_style` 与旧 Harness 版本不是仓库发布号，不能互相替代。

## 2026-09-16：模块化流程与仓库维护更新

### 生产流程

- 发布九个核心模块，研究、配音、字幕、素材、逐句配画、配乐、渲染、封面由十三阶段总控路由。
- 固定 `workspace_paths.json`、稳定期号、阶段 `vNNN`、永久素材库与本地缓存；旧期接入保持原审批和实际文件位置。
- 固定狐久参考声音、准确文字、开头试配、审核 UI 和全片物理镜头复用检查。
- 支持批量登记真实审批、统一删段时钟、增量失效、独立 A/B/C 与音频交付，以及六稿封面和选定后的多画幅。
- 保留五个兼容/可选模块及旧 Harness，为历史引用标注适用范围。

### 安装与依赖

- 新增明确的模块清单、默认只预览且拒绝覆盖的安装器、独立虚拟环境说明和只读依赖检查。
- 新安装默认使用官方用户级目录 `~/.agents/skills`；已有安装可显式指定目的地，不自动搬迁。
- 分离基础 Pillow、开发 PyYAML、旧 HTTPS 辅助 certifi；模型引擎、权重与渲染服务继续按需单独准备。
- 删除未被读取的编辑器环境变量示例，标明旧配置需手动加载，以及当前目录使用 CLI 参数冻结。

### 修复与验证

- 狐久参考校验支持 `~/` 展开，仍核对真实字节、原文及试配绑定。
- 封面尺寸检查优先使用 Pillow，避免 Linux CI 依赖 macOS `sips`。
- 结构检查覆盖根文档及引用、JSON/YAML、完整模块清单与固定审核 UI 哈希；忽略本机编译缓存，仍拒绝被强制提交的缓存/媒体。
- 本地与 CI 共用 `tools/check.py`，使用独立测试进程与临时编译缓存；支持 macOS Swift typecheck。
- CI 更新到基于 Node 24 的官方 Actions 固定提交，覆盖 Linux Python 3.11/3.14 和 macOS Python 3.14。
- README、安装/依赖文档、安全说明、贡献指南、Issue/PR 模板同步当前模块和公开仓库状态。

所有测试使用合成或脱敏数据。通过代码回归不表示某一期旁白、选片、正式视频或封面已经完成生产审批。
